This example illustrates the use of Googlre maps with Angular2.

Add a location (by name), then add marker. Each marker will be connected by a polyline.

To run:

- in a cmd window navigate to the root directory Lesson06
- run "npm start". This will open browser
- in the browser use http://localhost:3000/Lesson06


